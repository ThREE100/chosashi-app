# 記述式（第21問・第22問）学習漫画 ― ChatGPT自動操作ワークフロー

土地家屋調査士試験の**記述式**（第21問＝土地／第22問＝建物）過去問について、Claude Codeが
ブラウザ操作でChatGPTを直接動かし、学習漫画の画像を生成・検品・命名・ZIP化するまでの手順書。

**このファイルの位置づけ**：`note-articles/`配下の**択一式**note記事のワークフローとは完全に別物。
記述式の内部資料・図版・note記事そのものは、このリポジトリではなくローカルの
`Claude cowork`フォルダ（OneDrive同期）で管理している（下記「素材の置き場所」参照）。
このファイルは「どうやってChatGPTを操作するか」という**手順のノウハウ**だけをGitHubに残し、
他のPCでも同じ手順を再現できるようにするためのもの。

---

## 1. 全体の流れ

```
①作問済みの内部資料・図版（manga_zu_{年度}/）を用意する
　　→ 指示書_過去問コンテンツ制作*.md、指示書_第21問_土地.md、
　　　ChatGPT実行手順_学習漫画_{年度}第{21|22}問.md、確定値表、検算.py 等
　　→ 詳細は Skill kakomon（Claude cowork/.claude/skills/kakomon/SKILL.md）を参照

②年度・問題ごとにChatGPTのチャットを1つ作る（例：H29_21、H29_22）
　　→ 既存のチャットがある場合はサイドバーで再利用する（毎回新規に作らない）

③最初の1通でキャラクターを固定する
④2通目以降、1ページずつ図版を添付してプロンプトを送る
⑤生成のたびに「本当に画像が表示されたか」を必ず目視確認する【最重要・§4】
⑥全ページ完成後、ChatGPT自身にリネーム＋ZIP化させてダウンロードする【§6】
```

## 2. 環境・ツール

- ブラウザ操作は **`mcp__claude-in-chrome__*`**（Claude in Chrome＝ユーザーの実ブラウザ）を使う。
  `mcp__Claude_Browser__*`（アプリ内ブラウザ）は**ファイルアップロード機能を持たない**ため、
  画像添付が必要なこの作業には使えない。
- ChatGPTに実ブラウザでログイン済みであること（Plus等、画像生成が使えるプラン）。
- モデル設定は指示書に従う（例：GPT-5.6、画像出力「中程度」）。チャットごとに設定が
  異なる場合があるので、新規チャット作成時は毎回確認する。

### ファイルアップロードの制約

`mcp__claude-in-chrome__file_upload` は、**このセッションが読み取り許可を持つパス
（scratchpad／作業ディレクトリ等）にあるファイルしかアップロードできない。**
OneDriveやGoogleドライブ上のファイルを直接指定するとエラーになるため、
先にBashで一時ディレクトリ（scratchpadの下）へコピーしてから、そのコピーをアップロードする。

```bash
mkdir -p "<scratchpad>/figs"
cp "<manga_zu_{年度}のパス>/K01_xxx.png" "<scratchpad>/figs/"
```

### 日本語入力の落とし穴

- **`type`アクションの文字列に改行（`\n`）を埋め込むと、送信途中でEnterとして解釈され、
  メッセージが分割・重複送信されることがある。** 命名ルールなど複数行の指示を送るときは、
  改行を使わず句点「。」や読点で区切った1行の文章にする。
- **全角のＡ・Ｂ・Ｃなどが、タイピング時にÃ・Â等の文字化けした文字に化けることがある。**
  ChatGPT側は指示書の正規表記（Ａ市Ｂ町等）に自動で判断して補正してくれることが多いが、
  **最終画像で実際にどう表示されたか必ず確認する。**（判断に任せきりにしない）

## 3. 最初の1通（キャラクター固定）

`ChatGPT実行手順_学習漫画_{年度}第{21|22}問.md` の「1. 最初の1通」をそのまま使う。
参照画像は年度をまたいで共通の以下5枚（**唯一の基準**。年度ごとに作り直さない）。

```
置き場所：G:\マイドライブ\キャラ仕様書\
  ChatGPT Image 2026年8月26日 10_56_45.png   … 顔向き8カット（基本）
  ChatGPT Image 2026年8月30日 12_55_01 (1).png … 顔向き8カット（差分1）
  ChatGPT Image 2026年8月30日 12_55_01 (2).png … 顔向き8カット（差分2）
  ChatGPT Image 2026年9月2日 03_39_29 (1).png  … 全身ポーズ・歩き
  ChatGPT Image 2026年9月2日 03_39_29 (2).png  … 手のカット・小道具
  女性キャラクター_統一仕様書.md               … 上記画像の使い方の仕様書
```

既存チャットで問1編・問2編等が完成済みの場合、通常はこの5枚を再送する必要はない
（同じチャット内で参照が保持されている）。ただし§5のキャラクターぶれが起きた場合は、
この5枚を**「今後の唯一のキャラクター基準」として明示的に再読み込みさせる**。

## 4. 1ページごとの生成と検品【最重要】

`ChatGPT実行手順_....md` の「2. 図のあるページのプロンプト（共通テンプレ）」に沿って、
`{ページ番号}` `{図のファイル名}` `{セリフ}` `{結論の1行}` を埋めて1ページずつ送る。
図版なしページ（各編の最終ページ等）も手順書の該当プロンプトをそのまま使う。

### 生成後は必ず「実際に画像が表示されたこと」を確認する

**ChatGPTは「〜を作成しました」「完成画像を表示しました」とテキストで報告しても、
実際には画像が1枚も生成されていない（または内部ワークスペースの整理で参照ファイルが
失われている）ことがある。** このセッションで実際に踏んだ事故：

```
・「P1を作成しました」の直後に、添付した元図版がそのままエコーされているだけで、
  実際のキャラクター入り漫画ページが一度も表示されていなかった
・「画像生成機能が利用上限に達したため、今回は生成できませんでした」と言いながら、
  そのまま次のページに進んでしまっていた
・「既存のP1完成画像を再表示します」と言われて実行させても、画像が一切出てこなかった
  （本人いわく「画像枚数制限で実行されていない」）
```

そのため、各ページごとに次のチェックを徹底する。

```
[ ] 生成メッセージの直後に、実際にキャラクター入りの完成ページ画像が表示されているか
    （元図版だけがそのままエコーされているのは不可）
[ ] 画像内のセリフ・数値・ラベルが、指示した文言・確定値表の数値と一字一句一致するか
[ ] キャラクターの顔・髪型・服装が、これまでのページ・§3の参照画像と同じに見えるか
```

1つでも欠けていたら、「以前の報告は破棄する。同じ基準でP{n}を作り直す。作成後は必ず
画像をこのチャットに表示すること」と明示して**作り直す**（生成が完了するまで次のページに
進まない）。「完成した」という自己申告だけで次に進まない。

## 5. キャラクターがぶれた場合の復旧手順

長いセッションでは、内部ワークスペースの整理により参照画像や生成済みファイルが
失われ、以降のページで別人のようなキャラクターが生成されることがある。

```
1. §3の5枚（G:\マイドライブ\キャラ仕様書\）を再アップロードし、
   「今後の唯一のキャラクター基準として再読み込みしてください」と明示する
2. 該当セッションで作成した全ページを対象に、顔が新基準と異なるものを
   「先にリストで報告させ、まだ作り直さない」よう指示する
   （ChatGPTの自己申告は不正確なことがあるため、リストは参考程度に留め、
   最終的には自分で1ページずつ見比べる）
3. 疑わしい編（問N編）は丸ごと「以前の完成報告を破棄し、P1から作り直す」と指示し、
   §4の検品を1ページずつ徹底しながら再生成する
```

## 6. 完成後：命名とZIP化はChatGPT自身にやらせる

30枚を超えるページを1枚ずつ手動ダウンロードするのは非常に遅い（画像エディタの
サムネイル一覧→ダウンロードアイコンをクリックする方式は動くが、数十枚には非現実的）。
**ChatGPTの同じ会話に「リネーム＋ZIP化」を指示し、Work機能内のサンドボックスで
処理させてダウンロードリンクを出させる方が圧倒的に速い。**

### 命名ルール

```
年度_編_ナンバリング（2桁ゼロ埋め）

第21問（土地）＝3セクション構成の場合
  問1編          P1〜P{n}  → H{年度}_1_01 〜
  問2・問3編（結合） P1〜P{n}  → H{年度}_2・3_01 〜
  問4編          P1〜P{n}  → H{年度}_4_01 〜

第22問（建物）＝3セクション構成の場合
  問1編 → H{年度}_1_01〜　　問2編 → H{年度}_2_01〜　　問3編 → H{年度}_3_01〜
```

（セクション構成は年度によって多少異なるので、その年度の`ChatGPT実行手順_....md`の
目次と照らし合わせて番号を決める。）

### 指示文の例（実際に機能したプロンプト）

```
問1編・問2編・問3編で作成した全のページ画像（全１４枚）を、以下の命名ルールで
リネームし、１つのZIPファイルにまとめて、ダウンロードできる形で提供してください。
命名ルールはこちらです。問1編P1〜P4はH29_1_01、H29_1_02、H29_1_03、H29_1_04。
問2編P1〜P5はH29_2_01、H29_2_02…（以下同様、読点区切りで改行を使わない）。
各P番号は作成した順番で対応します。拡張子は.pngのままです。このルールで全14枚を
リネームし、ファイル名はH29_22_建物_完成画像.zipとして、ダウンロードできる形で
提供してください。
```

- 命名ルールを最初から詳しく書かずに送ると「命名ルールの記載が確認できませんでした」
  と聞き返されることがある。その場合は上記のように詳細を1行（改行なし）で送り直す。
- 「一部の画像がワークスペース整理で移動・削除されており確定できない」等のエラーが
  途中で出ても、多くの場合ChatGPT自身が実ファイルを再照合して復旧する。生成し直しを
  提案してきた場合は、既存ファイルの範囲で解決できないか確認してから許可する
  （§4の事故と同種のリスクがあるため）。
- 完成すると、メッセージ内に `📎 {ファイル名}.zip` というリンクが表示される。
  **リンクをクリックしただけでは自動ダウンロードされない場合がある。** その場合は
  リンクをクリックして開いたプレビュー画面右上の「…」→「ダウンロードする」を押す。

### ダウンロード先の確認

実ブラウザ（Claude in Chrome）のダウンロード先は、Chromeの既定である
`Downloads`フォルダとは限らない（このセッションでは`OneDrive\Desktop\`になっていた）。
ダウンロードが完了したはずなのにファイルが見当たらないときは、両方を確認する。

```bash
find "<Downloads>" -maxdepth 1 -iname "*.zip" -newermt "<開始時刻>"
find "<OneDrive>\Desktop" -maxdepth 1 -iname "*.zip" -newermt "<開始時刻>"
```

### 受け取り後の検品

ZIPを展開し、①ファイル数が想定枚数と一致するか、②ファイル名が命名ルールどおりか、
③2〜3枚を実際に開いて内容（キャラクター・セリフ・数値）が正しいかを確認する。

## 7. 素材の置き場所（このリポジトリの外・OneDrive/Googleドライブ）

記述式の内部資料・図版・note記事そのものは、このGitHubリポジトリには含めていない
（対象がこのリポジトリの`chosashi-app`＝択一式学習アプリの管轄外のため）。
OneDrive／Googleドライブ経由で同じアカウントにログインすれば、他のPCからも
同じパスで参照できる。

```
指示書・ChatGPT実行手順・note記事・確定値表・検算.py・図版対応表
  → OneDriveの「Claude cowork」フォルダ直下
    （Skill kakomon: Claude cowork/.claude/skills/kakomon/SKILL.md から入る）

図版（manga_zu_{年度}/ 一式）
  → OneDriveの「ドキュメント\ChatGPT\note_土地家屋調査士試験_漫画\assets\」にも
    ミラーがある（compose_pages.py 等、SVG合成を試す場合はこちら）

キャラクター参照画像・仕様書（§3の5枚）
  → Googleドライブの「キャラ仕様書」フォルダ

問題PDF・アガルート解説PDF
  → Googleドライブ（年度別フォルダ）
```

## 8. 既知の未解決事項

- ChatGPT側の「Work」モードは、長時間のセッションで内部ワークスペースのファイルが
  時間経過とともに整理・失効することがある（§4・§6のエラーの根本原因と推測される）。
  現時点で確実な回避策はなく、**生成のたびに実際の画像表示を確認する**運用でカバーしている。
- 「このシリーズの画像をX枚ダウンロード」という一括ダウンロードのメニューも存在するが、
  挙動が安定しないため、本手順では§6の「ChatGPTにリネーム＋ZIP化させる」方式を正としている。
