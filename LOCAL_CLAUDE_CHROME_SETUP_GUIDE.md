# ローカルPC + Claude Code + Chrome拡張で、ブラウザ自動操作の一括生成タスクを回す手順

ChatGPT・その他のWEB版ツールを使って「大量のプロンプトを順次流し込み、生成・検品・
リネーム・ZIP化まで自動でやらせる」タスクを、ローカルPC上のClaude Code（「Claude in
Chrome」拡張機能）で実行するための**汎用セットアップ手順**。特定のプロジェクトや案件に
依存しない内容なので、他のリポジトリ・他の案件でもこのファイルをコピーして使える。

**このファイルの位置づけ**：`CHATGPT_MANGA_WORKFLOW.md`・`CHATGPT_INFOGRAPHIC_BATCH_WORKFLOW.md`
のような「何を・どんなプロンプトで生成するか」を定めた**案件固有の手順書**とは別物。このファイルは
「そもそもローカル環境をどう用意し、どうやってその手順書をローカルのClaude Codeに実行させるか」
という**環境構築・運用パターン**だけをまとめたもの。案件固有の手順書は、このファイルの手順で
立ち上げたローカルのClaude Codeに読み込ませて使う。

---

## 1. 全体の構図（2セッション体制）

このパターンは、性質の異なる2つのClaude Codeセッションを使い分けることで成り立つ。

```
┌─────────────────────────────┐      ┌──────────────────────────────────┐
│ クラウド上のリモートセッション        │      │ ローカルPC上のセッション              │
│ (Claude Code on the web 等)   │      │ (claude --chrome で起動)          │
│                              │      │                                  │
│ ・実ブラウザに接続できない          │      │ ・ユーザーの実Chromeを直接操作できる     │
│ ・リポジトリの調査・スクリプト作成・   │ ──▶  │  （Claude in Chrome拡張機能）        │
│   案件固有の手順書(md)の作成・      │ 手順書 │ ・ChatGPT等のWEB版を開いて          │
│   GitHubへのコミット/push        │  を渡す │   ログイン・プロンプト送信・画像生成    │
│                              │      │   ・ダウンロードを実行できる            │
└─────────────────────────────┘      └──────────────────────────────────┘
```

- **リモート側の役割**：対象データの調査、プロンプトや対象一覧を取り出すスクリプト、
  検品スクリプト、案件固有の手順書（`CHATGPT_*_WORKFLOW.md`）を作り、GitHubにpushする。
  実ブラウザ操作はできないため、ここでは絶対に完結しない。
- **ローカル側の役割**：リポジトリをcloneし、案件固有の手順書を読み込んだうえで、
  実際にChromeを操作してWEB版ツールを動かす。ログインやCAPTCHA等、人間の判断が
  必要な場面はユーザーが画面を見ながら手動対応する。

## 2. ローカルPCの前提環境

以下がローカルPC（Windows想定。macOS/Linuxも基本同じ）に必要。

- **Git**：リポジトリのclone・pull用。
- **Python 3**：対象一覧の抽出・検品スクリプト用。
- **Claude Code CLI**：`claude --chrome` でブラウザ操作を行う本体。
- **Google Chrome + 「Claude in Chrome」拡張機能**：実ブラウザ操作用。
- **Claude Codeの直接Anthropicプラン（Pro/Max/Team/Enterprise）でのログイン**：
  Chrome連携はAPIキーでは使えない。Bedrock/Google Cloud/Microsoft Foundry経由も不可。
- **対象WEBツール（ChatGPT等）に実ブラウザでログイン済みであること**。

## 3. セットアップ手順（Windows / PowerShell）

### 3-1. Gitの確認

```powershell
git --version
```
入っていなければ https://git-scm.com/download/win からインストール。

### 3-2. リポジトリのclone

```powershell
cd C:\Users\<ユーザー名>\<任意の作業フォルダ>
git clone <リポジトリのURL>
cd <cloneしたフォルダ名>
```
以降のコマンドは、必ずこのフォルダの中で実行する。

### 3-3. Pythonの確認（Windows特有の落とし穴に注意）

```powershell
python --version
```

**`Python`とだけ表示されて終わる場合**：実際のPythonがインストールされておらず、
`python`（および`python3`）というコマンド名がMicrosoft Storeへの案内スタブ
（App Execution Alias）に乗っ取られている。この場合は次の手順でインストールする。

1. https://www.python.org/downloads/ からインストーラーを取得
2. インストーラーの最初の画面で **「Add python.exe to PATH」に必ずチェック**
3. インストール中、以下のような確認が出ることがあるが、いずれも**「y」（Yes）でよい**。
   - 「長いパス（260文字超）のサポートを有効にするか」→ y（管理者承認・再起動が必要になる）
   - 「python3.x.exe 等のコマンドをPATHに追加するか」→ y
   - 「最新版のCPythonをインストールするか」→ y（デフォルトでYes）
4. 長いパスのサポートを有効にした場合は、**PCを再起動**する（反映のため）。
5. 新しいPowerShellウィンドウを開き直し、`python --version` を再実行して
   `Python 3.x.x` のように表示されることを確認する。

`py --version` は環境によっては使えない（`py`コマンド自体が存在しないことがある）ので、
基本は `python` コマンドを使う。

### 3-4. 必要なPythonパッケージのインストール

案件固有の手順書が要求するパッケージを入れる（例：画像検品にPillowを使う場合）。

```powershell
pip install pillow
```

### 3-5. Pythonスクリプトの文字コードに関する注意（Windows特有）

Windows既定のロケールは `cp932`（Shift-JIS系）であり、日本語プロンプト中の記号
（emダッシュ「―」等、Shift-JISに存在しない文字）を標準出力に書き出そうとすると
`UnicodeEncodeError` になることがある。特に `python script.py > out.txt` のように
**リダイレクトした場合に顕在化しやすい**（ターミナルへの直接表示では発生しないことが多い）。

このリポジトリで使うPythonスクリプトを新規に書く／流用する場合は、スクリプト冒頭で
明示的にUTF-8へ切り替えておくと事故を防げる。

```python
import sys
if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8")
```

### 3-6. Claude Code CLIの確認

```powershell
claude --version
```
表示されない場合は https://code.claude.com/docs/en/chrome の手順に従ってインストールする。

### 3-7. Chrome拡張機能「Claude in Chrome」の確認

Chromeに拡張機能が入っていない場合は、次の3-8の起動時に案内が出ることが多い。
出ない場合は https://code.claude.com/docs/en/chrome から個別にインストールする。

### 3-8. ローカルのClaude Codeを起動

```powershell
cd C:\Users\<ユーザー名>\<cloneしたフォルダ>
claude --chrome
```

起動時に、順に次のような確認が出ることがある。いずれも自分が作成・信頼している
リポジトリでの作業であれば、下記のとおり進めてよい。

1. **「このフォルダを信頼するか」**（セキュリティ確認）
   → `Yes, I trust this folder` を選択。
2. **「Claude in Chromeの機能説明」**
   → そのままEnterで確認。
3. 直接のAnthropicプランでのログインを求められた場合はログインする。

## 4. 案件固有の手順書を実行させる

ローカルのClaude Codeが起動したら、案件固有の手順書（例：`CHATGPT_INFOGRAPHIC_BATCH_WORKFLOW.md`）
を読み込ませて、実行を指示する。プロンプトの型は次の通り。

```
{案件固有の手順書ファイル名}を読み込んで、{対象範囲・単位}からこの手順で進めてください。
{必要なツール設定（モデル名・モード等）があれば明記する}。
```

例（今回のインフォグラフィック一括生成の場合）：

```
CHATGPT_INFOGRAPHIC_BATCH_WORKFLOW.mdを読み込んで、R7年度の問1〜問3(3枚)からこの手順で
進めてください。ChatGPTのWorkワークスペースで、GPT-5.6・Luna・reasoning effort高いを
使ってください。
```

最初の一区切り（今回は年度単位＝3枚）が問題なく完了したら、残りを続けて実行させる指示を
同じセッションに追加で送ってよい。

```
続けて、{次の単位}まで、同じ手順で生成してください。各単位が完了するたびに(検品・リネーム・
ZIP化まで済ませたら)、次の単位へ自動的に進んでください。途中で不備があれば、その場で
再生成してから次に進んでください。全て完了したら、完了報告をしてください。
```

## 5. 実行中に想定される確認・操作

- **「Read outside the working directories」等の権限確認**：Claude Codeが一時ファイル
  （Tempフォルダ上のプロンプト退避ファイル等）を読み書きするために出ることがある。
  自分自身が動かしているセッションが自分のTempフォルダを読むだけの操作であれば、
  「Yes, keep allowing reads」を選んで進めてよい（同じ確認が繰り返し出るのを防げる）。
- **ChatGPT等のログイン・認証（Google認証・2段階認証・CAPTCHA等）**：Claude Codeは
  この種の認証ページに来ると自動的に一時停止し、手動操作を求めてくる。画面を見ながら
  自分で操作する。
- **長時間の自動実行**：数十件規模のバッチになる場合、ワークスペースの内部状態が
  長時間セッションで失効することがある（`CHATGPT_MANGA_WORKFLOW.md` §8参照）。
  案件固有の手順書側で「単位ごとに区切って都度完了させる」運用にしておくと、
  トラブル時も完了済み分はそのまま成果物として確定できる。
- **完了後の受け取り検品**：ダウンロードされたファイル（ZIP等）は、機械的な検品
  スクリプト（枚数・破損・透過チェック等）と、2〜3件の目視確認の両方を行う。

## 6. このパターンを新しい案件に転用する場合のチェックリスト

新しい案件でこのパターンを使うときは、次を用意する。

```
[ ] 対象データ（生成すべきプロンプト・素材）をリポジトリ内、または参照可能な場所に用意する
[ ] 対象一覧を機械的に取り出すスクリプト（対象データが多数ある場合）
[ ] 案件固有の手順書(CHATGPT_*_WORKFLOW.md等)：
    - 環境・ツール（使うWEBサービス、モデル設定等）
    - 1件ごとの生成と検品の基準（何を確認すれば「成功」とみなすか）
    - 命名ルール
    - 完成後のまとめ方（リネーム・ZIP化等）とダウンロード後の検品方法
    - 一度に処理する単位（長時間セッションでの失効を避けるための区切り方）
[ ] 検品スクリプト（ダウンロード後の機械チェック用）
[ ] このファイル（LOCAL_CLAUDE_CHROME_SETUP_GUIDE.md）へのリンクを、案件固有の手順書または
    プロジェクトのCLAUDE.md等に記載し、次にこの作業をする人（自分自身を含む）が迷わず
    たどり着けるようにする
```
