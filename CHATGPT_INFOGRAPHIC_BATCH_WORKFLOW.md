# R7〜H20 問1〜問3 ⑤作図ガイド型インフォグラフィック ― ChatGPT一括生成ワークフロー

`note-articles/{年度}-mondai/q01〜q03-*.md`（土地家屋調査士試験・民法の問1〜問3、全54記事）に
既に埋め込まれている「⑤作図ガイド型」インフォグラフィックのプロンプト（`infographic-prompt-template.md`
準拠）を、ChatGPT（WEB版）で順次自動生成し、透過チェック・リネーム・ZIP化までを行う手順書。

**このファイルの位置づけ**：`CHATGPT_MANGA_WORKFLOW.md`（記述式・第21/22問の学習漫画）と同じ
`mcp__claude-in-chrome__*`（Claude in Chrome＝ユーザーの実ブラウザ）を使うが、対象は択一式
note記事（`note-articles/`配下）であり、リポジトリ内で完結する点が異なる（外部フォルダの参照画像・
アップロードは不要。プロンプトはすべてテキストで、記事ファイルからコピペするだけでよい）。

**重要**：この手順はローカル環境（Chrome拡張「Claude in Chrome」が使えるPC上のClaude Code、
`claude --chrome` で起動）でのみ実行できる。クラウド上のリモートセッション（Claude Code on the web）
には実ブラウザが接続されていないため、この手順は実行できない。

---

## 1. 全体の流れ

```
①note-articles/tools/list_gozu_guide_targets.py で、54件の対象一覧（通し番号・年度・
  記事slug・出力ファイル名・プロンプト本文）を取得する
②ChatGPTで「Work」ワークスペースを開き、モデル設定を確認する
  （GPT-5.6、Terra、reasoning effort＝中程度）
③1件ずつプロンプトを貼り付けて生成し、そのつど実際に画像が表示されたか・背景が
  不透明か・パネル数が5枚そろっているかを目視確認する【最重要・§4】
④透過や欠落があれば、その場で再生成してから次に進む（1枚ずつ完了させる。
  ダウンロードは都度行わない）
⑤54件すべて完了したら、ChatGPT自身にリネーム＋ZIP化させてダウンロードする【§6】
⑥ダウンロード後、note-articles/tools/verify_infographic_zip.py で機械的に検品する
```

## 2. 環境・ツール

- ブラウザ操作は **`mcp__claude-in-chrome__*`** を使う（`mcp__Claude_Browser__*` はアプリ内
  ブラウザで、この作業ではどちらでも良いが、既存のChatGPTログインセッションを使い回せる
  実ブラウザ側が確実）。
- ChatGPTに実ブラウザでログイン済みであること（Work機能・画像生成が使えるプラン）。
- モデル設定：**GPT-5.6、Terra、reasoning effort＝中程度**。新規チャット作成時は毎回確認する
  （`CHATGPT_MANGA_WORKFLOW.md` と同様、チャットごとに設定が異なる場合がある）。
- 対象一覧の取得：

```bash
python3 note-articles/tools/list_gozu_guide_targets.py > /tmp/gozu_guide_targets.json
# 特定の1件のプロンプト本文だけを取り出す場合
python3 note-articles/tools/list_gozu_guide_targets.py --prompt-only 1
```

プロンプト本文は各記事ファイルの `## インフォグラフィック プロンプト（ア〜オ 作図ガイド）`
（または同等の見出し）直下のコードブロックにも直接あるので、`--prompt-only` を使わず
記事ファイルを直接開いてコピーしてもよい。

## 3. チャットの作り方

- 54件を1つの長いチャットで続けると、`CHATGPT_MANGA_WORKFLOW.md` §8で既知の問題として
  挙げられている「Workモードは長時間セッションで内部ワークスペースのファイルが整理・
  失効することがある」リスクが高まる。**年度ごと（3記事＝3枚）に新しいチャットを分ける**
  ことを推奨する（例：チャット「R7_問1-3」「R6_問1-3」…）。
- 各年度チャットの最初の1通で、モデル設定（GPT-5.6・Terra・reasoning effort中程度）と
  「これから3件、それぞれ独立したインフォグラフィックを作成する。前の画像を踏襲する必要は
  ない」ことを明示してから開始する。

## 4. 1件ごとの生成と検品【最重要】

1件のプロンプト（記事1本分の⑤コードブロック全文）をそのまま貼り付けて送信する。

### 生成後は必ず次を確認する

```
[ ] 実際に画像が表示されているか（テキストで「作成しました」とだけ報告して画像が
    出ていないケースがある。CHATGPT_MANGA_WORKFLOW.md §4と同じ事故に注意）
[ ] パネルが5枚そろっているか（多い/少ない/重複していないか）
[ ] 背景が画像の端から端まで完全に不透明か（チェッカーボード模様・透過部分がないか）
    → 疑わしい場合は、異なる背景色のページに一時的に表示して確認するか、
      「背景が透過していないか確認してください」とChatGPTに再確認させる
[ ] タイトル・サブタイトルの年度・問題番号が、その記事のものと一致しているか
    （年度チャットをまたいで前の年度のタイトルを使い回してしまう事故を防ぐ）
[ ] 簡体字・文字化けが疑われる漢字がないか（プロンプトのFinal checkで指定した漢字を中心に）
```

1つでも問題があれば、「この画像は背景が透過している（またはパネル数が違う／文字が
おかしい）。同じプロンプトで作り直してください」と明示して**その場で再生成する**
（次の記事のプロンプトに進まない）。「完成した」という自己申告だけで次に進まない。

### 背景透過が疑われた場合の再生成テンプレート

```
この画像は背景に透過（アルファチャンネル）が残っている可能性があります。
同じ内容・同じプロンプトで、背景を画像の端から端まで完全に不透明にして作り直して
ください。チェッカーボード模様や透過部分が少しでも残っていないか、生成後に
自己確認してから提示してください。
```

## 5. 命名ルール

`list_gozu_guide_targets.py` が出力する `output_filename` をそのまま使う。

```
{通し番号2桁}_{年度}-{記事slug}.png

例：
  01_r7-q01-ishihyouji.png
  02_r7-q02-senyuken.png
  ...
  54_h20-q03-mukou-torikeshi.png
```

年度をまたいで `q01`〜`q03` というslugが重複する（例：`q01-ishi-hyouji` はr4とh23の両方に
存在）ため、**年度プレフィックスを必ず付ける**。通し番号は「どの記事に対応するか」の
突き合わせを楽にするためのものであり、`list_gozu_guide_targets.py` の出力と1対1対応する。

## 6. 完成後：命名とZIP化はChatGPT自身にやらせる

`CHATGPT_MANGA_WORKFLOW.md` §6と同じ理由（1枚ずつ手動ダウンロードは非現実的）で、
ChatGPTの同じ会話（年度ごとに分けた場合は、それぞれのチャット、または最後にまとめ役の
チャットを1つ作って全画像を集約）に「リネーム＋ZIP化」を指示する。

### 指示文の例

```
このチャットで作成した3枚の画像を、以下のファイル名にリネームして、1つのZIPファイルに
まとめ、ダウンロードできる形で提供してください。1枚目は01_r7-q01-ishihyouji.png、
2枚目は02_r7-q02-senyuken.png、3枚目は03_r7-q03-souzoku.pngとします（読点区切りで
改行を使わない）。拡張子は.pngのままです。ZIPファイル名はr7_gozu_guide.zipとして、
ダウンロードできる形で提供してください。
```

- 年度ごとに分けた場合は、最後に全ZIPをローカルでまとめて1つに結合してもよいし
  （`zip -j all.zip *_gozu_guide.zip` 展開後に再zip、等）、そのまま年度別ZIPとして
  扱ってもよい。ユーザーの希望に合わせる。
- 「一部の画像がワークスペース整理で移動・削除されており確定できない」等のエラーが
  出た場合の対応は `CHATGPT_MANGA_WORKFLOW.md` §6と同じ（既存ファイルの範囲で解決
  できないか確認してから、生成し直しの提案を許可する）。
- ダウンロード先は既定の`Downloads`フォルダとは限らない（`CHATGPT_MANGA_WORKFLOW.md`
  §6「ダウンロード先の確認」を参照）。

## 7. 受け取り後の検品

```bash
python3 note-articles/tools/verify_infographic_zip.py <ダウンロードしたzipのパス> --expected 54
# 年度ごとに分けてダウンロードした場合は --expected 3
```

このスクリプトは次を機械的に確認する。

- ZIPが破損していないか（`testzip()`）
- 期待枚数と一致するか
- 各PNGが完全に不透明か（アルファチャンネルがある場合、全ピクセルalpha=255か。
  1ピクセルでも透過していればNGとして報告する）

機械チェックが通っても、**2〜3枚は実際に開いて内容（年度・問題番号・パネル数・
文字化けの有無）を目視確認する**（§4のチェック項目を参照）。

## 8. 進め方の目安

52記事（既に⑤プロンプトが作成済みの2記事＝R1問1・H30問3を除く）× 1枚＝52枚を
一度に生成しようとすると、§3の「長時間セッションでのワークスペース失効」リスクが
大きくなる。**年度ごと（3枚）に区切り、各年度の完了時点でZIP化・検品まで済ませてから
次の年度に進む**運用を基本とする（途中で中断しても、完了済みの年度分はそのまま成果物
として確定できる）。
